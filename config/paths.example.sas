/* Set absolute paths using forward slashes. */
%let KHANES_DIR=C:/ResearchData/suicide-indicators-2018-korean-national-surveys/khanes;
%let KOWEPS_DIR=C:/ResearchData/suicide-indicators-2018-korean-national-surveys/koweps;
%let MORTALITY_FILE=C:/ResearchData/suicide-indicators-2018-korean-national-surveys/apc2018.XLSX;

/* Each run creates its own output folder. */
%let OUTPUT_ROOT=&PROJECT_ROOT/outputs;
